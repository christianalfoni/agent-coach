{
  "name": "claude-debrief",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "claude-debrief": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node && printf '#!/usr/bin/env node\\n' | cat - dist/index.js > dist/tmp.js && mv dist/tmp.js dist/index.js && chmod +x dist/index.js",
    "prepublishOnly": "bun run build",
    "test": "bun test"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@clack/prompts": "^1.3.0",
    "chalk": "^5.6.2"
  }
}
